# This repo holds only intended config; guard against accidental drops.
*.log
.DS_Store
tmp/
*.local.json

# Never commit credentials/auth that might get copied in.
auth.json
.env

# Per-task worktree root the orchestrator creates under each feature repo (never commit it).
.dev-loop/
